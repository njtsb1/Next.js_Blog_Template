# Creating Your Personal Blog with Next.js

Project developed at Bootcamp React Developer Training with guidance from specialist [Pablo Henrique](https://github.com/pablohdev "Pablo Henrique").
In this challenge, you will use Next.js to build your personal blog! Next.js is a React framework that brings together various features—such as hybrid and static content rendering, TypeScript support, a routing system, feature packages, and a wide range of plugins and examples—to accelerate development by providing a complete structure for your project.
Mastering Next.js is a great way to stand out in the front-end market.
You will also learn how to make your blog dynamic and integrate it with a cloud service.

## Technologies used

- **HTML** — semantic markup (header, main, section, aside, footer).
- **CSS** — styles with CSS variables, dark (default) and light themes.
- **JavaScript** — logic for switching themes, changing languages, and rendering posts; persists preferences in `localStorage`.

## Features

- **Dark/Light Mode** with icons (moon/sun). **Dark** mode is the default.
- **Multilingual**: EN-US (default), PT-BR, ES.
- **Accessibility**: semantic elements, `aria-*`, visible focus, keyboard-accessible controls.
- **Responsiveness**: adaptive layout for desktop, tablet, and smartphone.

## How to use

1. Open `index.html` in your browser (double-click or `Ctrl+O`).
2. Use the language selector at the top to switch between EN / PT-BR / ES.
3. Use the theme button (moon/sun) to switch between dark and light. Preferences are saved in `localStorage`.

## Best practices and suggested extensions

- Add more posts to the `translations` object in `script.js`.
- Separate components (e.g., create templates or use Web Components) for larger projects.
- Include responsive images with descriptive `srcset` and `alt` tags.
- Validate color contrast and test with screen readers.
- Implement pagination or filters (client-side) for many posts.

[LICENSE](/LICENSE)

See [original repository](https://github.com/digitalinnovationone/trilha-react-desafio-5)
